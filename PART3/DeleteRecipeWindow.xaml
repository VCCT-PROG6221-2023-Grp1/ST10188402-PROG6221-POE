<Window x:Class="PART3.DeleteRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Recipe" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Recipe Name:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="txtRecipeName" Grid.Row="1" Margin="10" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="btnDelete" Content="Delete" Click="btnDelete_Click" MinWidth="80" Margin="5"/>
            <Button x:Name="btnCancel" Content="Cancel" Click="btnCancel_Click" MinWidth="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
